// Import React and HelloText class
import React from 'react';

// Create class called WelcomeScreen that extends the base React Component class
class HORMap extends React.Component {
    render(props) {
        return (
            <div></div>
      )
    }
}

HORMap.propTypes = {
};

HORMap.defaultProps = {
};

export default HORMap;
